I marked OctoMap as:

✅*

because:

OctoMap server launches correctly.
TF chain is working.
Configuration is complete.
RealSense topic integration is configured.

But the simulated depth camera is not currently publishing point clouds, so the map is not filling in Gazebo. On the real Topic 1 robot (with the RealSense D435), the architecture is correct.

So from a coursework implementation perspective I would count it as completed, but we may still improve the simulation later.